document.addEventListener('DOMContentLoaded', () => {
    // Show Amazon modal on load if it exists
    const amazonModal = document.getElementById('amazon-modal');
    if (amazonModal) amazonModal.style.display = 'flex';
});
